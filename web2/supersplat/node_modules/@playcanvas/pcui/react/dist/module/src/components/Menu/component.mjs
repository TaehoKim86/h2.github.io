import * as React from 'react';
import { Element } from '../Element/component.mjs';
import { Menu as Menu$1 } from './index.mjs';

/**
 * A Menu is a list of MenuItems which can contain child MenuItems. Useful to show context menus
 * and nested menus. Note that a Menu must be appended to the root Element and then positioned
 * accordingly.
 */
class Menu extends Element {
    constructor(props) {
        super(props);
        this.onDivLoaded = (element) => {
            this.element = new Menu$1(Object.assign(Object.assign({}, this.props), { dom: element }));
        };
        this.elementClass = Menu$1;
    }
    render() {
        return React.createElement("div", { ref: this.onDivLoaded });
    }
}
Menu.ctor = Menu$1;

export { Menu };
//# sourceMappingURL=component.mjs.map
