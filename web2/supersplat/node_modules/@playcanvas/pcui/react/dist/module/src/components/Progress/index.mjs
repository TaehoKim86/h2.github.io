import { Container as Container$1 } from '../Container/index.mjs';
import { Element as Element$1 } from '../Element/index.mjs';

const CLASS_ROOT = 'pcui-progress';
const CLASS_INNER = `${CLASS_ROOT}-inner`;
/**
 * Represents a bar that can highlight progress of an activity.
 */
let Progress$1 = class Progress extends Container$1 {
    /**
     * Creates a new Progress.
     *
     * @param args - The arguments.
     */
    constructor(args = {}) {
        super(args);
        this._inner = new Element$1({
            class: CLASS_INNER
        });
        this.class.add(CLASS_ROOT);
        this.append(this._inner);
        if (args.value !== undefined) {
            this.value = args.value;
        }
    }
    /**
     * Sets the value of the progress bar. The range is from 0 to 100.
     */
    set value(val) {
        if (this._value === val)
            return;
        this._value = val;
        this._inner.width = `${this._value}%`;
        this.emit('change', val);
    }
    /**
     * Gets the value of the progress bar.
     */
    get value() {
        return this._value;
    }
};
Element$1.register('progress', Progress$1);

export { Progress$1 as Progress };
//# sourceMappingURL=index.mjs.map
