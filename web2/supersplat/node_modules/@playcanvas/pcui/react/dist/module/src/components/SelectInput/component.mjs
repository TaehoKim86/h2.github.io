import { Element } from '../Element/component.mjs';
import { SelectInput as SelectInput$1 } from './index.mjs';

/**
 * An input that allows selecting from a dropdown or entering tags.
 */
class SelectInput extends Element {
    constructor(props) {
        super(props);
        this.elementClass = SelectInput$1;
        this.onSelect = props.onSelect;
        this.onAttach = this.onAttachFn.bind(this);
    }
    onAttachFn() {
        if (this.props.onSelect) {
            this.element.on('select', this.onSelect);
        }
    }
    render() {
        return super.render();
    }
}
SelectInput.ctor = SelectInput$1;

export { SelectInput };
//# sourceMappingURL=component.mjs.map
