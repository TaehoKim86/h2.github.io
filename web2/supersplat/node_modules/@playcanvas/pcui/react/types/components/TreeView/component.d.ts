import * as React from 'react';
import { Element } from '../Element/component';
import { TreeViewArgs } from './index';
/**
 * A container that can show a TreeView like a hierarchy. The TreeView contains
 * TreeViewItems.
 */
declare class TreeView extends Element<TreeViewArgs, any> {
    parentElement: any;
    constructor(props: TreeViewArgs);
    loadChildren(children: any, element: any): void;
    componentDidUpdate(): void;
    parentElementRendered(element: any): void;
    render(): React.JSX.Element;
}
export { TreeView };
